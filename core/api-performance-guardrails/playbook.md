# Playbook

## Objective
Keep APIs predictable, fast, and observable for critical product flows.

## Rules
- Every endpoint requires an explicit contract.
- Standardize HTTP statuses and errorr payloads by domain.
- Define cache and invalidation before merge.
- Track minimum latency and failure metrics per route.
